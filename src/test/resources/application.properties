# Override production settings for slice tests (@DataJpaTest)
# Flyway is excluded by @DataJpaTest, but validate ddl-auto would still fail —
# let Hibernate create the schema from entity annotations instead.
spring.jpa.hibernate.ddl-auto=create-drop
spring.flyway.enabled=false
